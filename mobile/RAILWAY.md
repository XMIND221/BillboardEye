# URL API Railway

La valeur canonique est dans **`src/constants/railway.js`**.

Pour les builds EAS, copier la même URL dans **`eas.json`** → `env.EXPO_PUBLIC_RAILWAY_API_URL`.

Voir aussi la section *Railway* dans `DEPLOYMENT.md` à la racine du repo.
