/**
 * URL HTTPS publique de l’API déployée sur Railway.
 * À synchroniser avec `mobile/eas.json` (profils production / production-apk).
 * Même valeur que `admin/src/constants/railway.js`.
 */
export const RAILWAY_API_BASE_URL =
  "https://billboardeye-production.up.railway.app/api";
