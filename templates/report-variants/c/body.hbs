{{! Variante C — premium sobre (source v0) }}
<div class="report-variant-c" style="--accent: {{primaryCss}};">
  {{> cover}}
  {{> summary}}
  {{#if zones.length}}
  {{> zones}}
  {{/if}}
  {{> closing}}
</div>
