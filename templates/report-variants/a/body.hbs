{{! Variante A — editorial minimal (source v0) }}
<div class="report-variant-a" style="--accent: {{primaryCss}};">
  {{#if showCover}}
  {{> cover}}
  {{/if}}
  {{#if showSummary}}
  {{> summary}}
  {{/if}}
  {{#if showPanels}}
  {{#if zones.length}}
  {{> zones}}
  {{/if}}
  {{/if}}
  {{#if showClosing}}
  {{> closing}}
  {{/if}}
</div>
