{{! Variante B — corporate bold (source v0) }}
<div class="report-variant-b" style="--accent: {{primaryCss}};">
  {{> cover}}
  {{> summary}}
  {{#if zones.length}}
  {{> zones}}
  {{/if}}
  {{> closing}}
</div>
